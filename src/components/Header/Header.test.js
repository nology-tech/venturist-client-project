import { getAllByTestId, render,screen } from "@testing-library/react";
import Header from "./Header";

describe("Testing the Header", () => {

  test("Check to see if the Header renders", () => {
    // Arrange
    render(<Header />)

    // Act
    const header = screen.get

    // Assert
  })
})


// for h5
// for h1
// for p tag
// if all shows at the same time
